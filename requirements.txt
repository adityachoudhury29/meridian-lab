# Meridian Talent Ranker — dependencies (Python 3.12)
#
# NOTE on the compute constraint: the RANKING STEP (rank.py) imports only
# `numpy` + the standard library. The embedding model is used solely in the
# offline precompute (precompute.py) and the live demo (streamlit). So Stage-3
# reproduction of the ranking step has a tiny, network-free footprint.
#
#   ranking step only:   pip install numpy==2.1.3
#   full project:        pip install -r requirements.txt

numpy==2.1.3            # all vector math + the ranking step (only hard runtime dep)
model2vec==0.8.2        # static distilled embeddings (potion-retrieval-32M) — precompute & live demo
streamlit==1.58.0       # interactive demo + hosted sandbox (Streamlit Community Cloud)
pytest==9.0.3           # archetype / format / reasoning test suite
